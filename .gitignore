# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# uv
.venv/

# Manim render output
media/

# TTS cache (managed by KokoroService / test_tts.py)
.tts_cache/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# Model artifacts (use Git LFS or external storage for weights)
*.safetensors
*.bin
*.gguf
*.pt
*.pth
*.ckpt

# Large dataset dirs (raw + curated training data)
Datasets/raw/
Datasets/curated/

# Note: Datasets/eval/ and Models/eval_results/ are intentionally tracked
# (held-out evaluation data and benchmark results must be versioned).
